services:
  - type: web
    name: backnest
    env: node
    region: frankfurt
    plan: free
    buildCommand: "npm install && npm run build"
    startCommand: "npm run prestart && npm run start:prod"
    envVars:
      - key: DATABASE_URL
        fromService:
          type: postgres
          name: setupnest # 👈 must match your DB service name
          property: connectionString

databases:
  - name: setupnest
    plan: free
